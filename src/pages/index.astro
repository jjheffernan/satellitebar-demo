---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/sections/Hero.astro";
import FeatureSlot from "../components/FeatureSlot.astro";
import Testimonials from "../components/sections/Testimonials.astro";
import site from "../data/site.json";
import catalog from "../data/catalog.json";

const featured = catalog.packages.filter((p) => p.featured).slice(0, 3);
const sampleDrinks = catalog.cocktails.slice(0, 3);
---

<BaseLayout title={site.name} description={site.description} path="/">
  <Hero support="Packages + cocktail menu for weddings, corporate, and private events.">
    <FeatureSlot name="home.hero-media" />
  </Hero>

  <section class="hub container-site" aria-labelledby="order-heading">
    <div class="hub__head">
      <h2 id="order-heading">Packages</h2>
      <a href="/tools/drink-calculator">Drink calculator</a>
    </div>
    <ul class="hub__packages">
      {
        featured.map((pkg) => (
          <li>
            <a href={`/packages#${pkg.id}`}>
              <span class="name">{pkg.name}</span>
              <span class="meta">
                {pkg.guests} guests · {pkg.duration} · see what’s included
              </span>
            </a>
          </li>
        ))
      }
    </ul>
    <p class="hub__more"><a href="/packages">Compare packages &amp; pricing</a></p>

    <div class="hub__head hub__head--next">
      <h2 id="drinks-heading">Cocktails</h2>
      <a href="/menu">Full menu</a>
    </div>
    <ul class="hub__drinks" aria-labelledby="drinks-heading">
      {
        sampleDrinks.map((drink) => (
          <li>
            <span class="name">{drink.name}</span>
            <span class="meta">{drink.description}</span>
          </li>
        ))
      }
    </ul>

    <Testimonials />

    <div class="hub__head hub__head--next">
      <h2 id="cal-heading">Upcoming</h2>
    </div>
    <FeatureSlot name="home.events-calendar" />
  </section>
</BaseLayout>

<style>
  .hub {
    padding-block: 1.25rem var(--space-section);
  }

  .hub__head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.55rem;
  }

  .hub__head--next {
    margin-top: 1.35rem;
  }

  .hub__head a,
  .hub__more a {
    color: var(--primary);
    font-size: 0.85rem;
    text-decoration: none;
  }

  .hub__head h2 {
    margin: 0;
    font-size: 1.05rem;
    letter-spacing: -0.02em;
  }

  .hub__more {
    margin: 0.65rem 0 0;
  }

  .hub__packages,
  .hub__drinks {
    list-style: none;
    margin: 0;
    padding: 0;
    border-top: 1px solid var(--border);
  }

  .hub__packages li,
  .hub__drinks li {
    border-bottom: 1px solid var(--border);
  }

  .hub__packages a {
    display: grid;
    gap: 0.1rem;
    padding: 0.7rem 0;
    text-decoration: none;
  }

  .hub__drinks li {
    display: grid;
    gap: 0.1rem;
    padding: 0.65rem 0;
  }

  .name {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 0.98rem;
  }

  .meta {
    color: var(--muted-foreground);
    font-size: 0.82rem;
    line-height: 1.35;
  }

  .hub__packages a:hover .name {
    color: var(--primary);
  }
</style>
