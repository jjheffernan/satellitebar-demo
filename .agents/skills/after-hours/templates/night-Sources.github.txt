# Overnight Sources — GitHub-first
# Paste into `/after-hours` chat or Cursor Automation Instructions.
# Drain only labeled agent-ready issues (prefer Agent Brief over raw body).
# Do not enable wayfinder-afk or github-tickets here — those are opt-in only
# (see night-Sources.mixed.txt / Sources.example.txt).

/after-hours 45m
Sources:
  - github-issues: label ready-for-agent limit 5
  # Optional: further labels / smaller batch
  # - github-issues: label ready-for-agent limit 3
maxPrs: 3
priority: github-first
