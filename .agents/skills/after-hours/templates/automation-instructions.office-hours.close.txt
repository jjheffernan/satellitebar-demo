# Cursor Automation Instructions — office hours close (alpha)
#
# Paste into Automations → Instructions. Edit Sources / maxPrs for the target repo.
# Trigger: cron weekdays after office hours (example: 0 18 * * 1-5) — confirm time in the UI.
# Checkout: configured baseBranch from .cursor/after-hours-loop.config.json (must be committed).

You are running **after-hours** (alpha) as an unattended AFK tick.

Follow `.agents/skills/after-hours-loop/SKILL.md` end-to-end:
1. Preflight (config, gh, clean tree on baseBranch). Fail closed on hard preflight errors; write a short morning-style note in your final message if you stop.
2. Bootstrap Sources below (do not invent Sources).
3. Apply readiness; skip or block HITL / vague / grill-style work.
4. Execute at most `maxPrs` agent-ready items A→Z with the named executor.
5. Open **draft** PRs only (`draftPrs: true`). Never merge. Never force-push. Respect denylist.
6. Idempotency (cloud): if an open draft PR already covers an item (branch/title/body references the item id or issue), **skip** it — do not start a second implementation. If config `cloudLedgerPath` is set, also skip/update per `.agents/skills/after-hours-loop/references/cloud-ledger.md`.
7. Do not rely on `.cursor/after-hours-loop.state.json` from a previous run (usually absent in cloud). Prefer live Sources + existing PRs (+ ledger when configured).
8. End with a morning brief in your final message: done, blocked, PR links, human next steps.
9. Do not require grill / to-tickets before starting — Sources below are enough.

/after-hours bootstrap:
  - github-issues: label ready-for-agent limit 5
maxPrs: 2
priority: github-first

# Optional alternatives (replace the Sources block above):
# - todo-md: section "Now"
# - feature-spec: docs/plans/YOUR-PLAN.md phase 1
# Keep wayfinder-afk / github-tickets opt-in only when those peers exist and you know the map is AFK-safe.
#
# Do NOT add megaPr / CONFIRM_MEGA_PR here by default. Bundled mega-PR requires both tokens
# pasted into THIS fire's Instructions every time (references/mega-pr.md). Cron must not remember mega.
